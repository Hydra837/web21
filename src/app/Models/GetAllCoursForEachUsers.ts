export interface GetAllCoursForEachUsers {
  
  userNom?: string; // Le ? indique que la propriété est optionnelle
  userPrenom?: string;
  coursNom?: string;
  disponible: boolean;
  profNom?: string;
  profPrenom?: string;
}
  